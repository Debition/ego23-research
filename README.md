# Ego23 private research sharing

The public website contains a password entry screen and encrypted content. Research HTML, scripts, images, videos and data are encrypted together using AES-256-GCM and PBKDF2-SHA256. The password is never uploaded.

The Pages workflow publishes only `dist/`. Editable presentations and paper PDFs are excluded even from the encrypted content.
